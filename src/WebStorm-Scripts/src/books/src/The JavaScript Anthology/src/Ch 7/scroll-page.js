// Scroll down down 200 pixels
window.scrollBy(0, 200);

// Scroll across 200 pixels
window.scrollBy(200, 0);

// Scroll to 300 from the edge and 100 from the top
window.scrollTo(300, 100);

// Scroll to the beginning
window.scrollTo(0, 0);
